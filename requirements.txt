# ── tracking experiment ───────────────────────────────────────
wandb==0.17.0

# ── core ──────────────────────────────────────────────────────
torch==2.5.1
torchvision==0.20.1
numpy==1.26.4
opencv-python==4.9.0.80
Pillow==10.3.0
PyYAML==6.0.1
tqdm==4.66.4
scipy==1.13.1

# ── detection ─────────────────────────────────────────────────
ultralytics==8.4.45

# ── visualization ─────────────────────────────────────────────
matplotlib==3.9.0
pandas==2.2.2

# ── api ───────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9
pydantic==2.7.1

# ── deferred — install separately when needed ─────────────────
sahi==0.11.36                                         # version needs verification
# git+https://github.com/KaiyangZhou/deep-person-reid.git  # Python 3.12 compat uncertain
pycocotools==2.0.11                           # needs C++ Build Tools on Windows
# git+https://github.com/JonathonLuiten/TrackEval.git      # needed at evaluation stage
# openvino                                     # needed at export stage only